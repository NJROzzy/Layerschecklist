import type { CourseLessonData } from "./types";
export const week13: CourseLessonData = {
 week:13,title:"Deep Reinforcement Learning",lead:"An agent's predictions now influence its future data. Learn how rewards become value targets, why deep Q-learning needs stabilizers, and how to evaluate behavior rather than only a training loss.",prerequisites:"Expected values, neural regression, gradient descent, and careful evaluation.",outcomes:["Describe an MDP and distinguish rewards, returns, values, and policies.","Calculate Bellman targets and temporal-difference updates.","Implement DQN with replay, a target network, and correct terminal handling.","Explain policy gradients and actor–critic, then design a behavioral evaluation."],
 sections:[
 {id:"interaction",title:"Learning changes the data collection process",paragraphs:["In reinforcement learning, an agent observes a situation, chooses an action, and receives a reward and a new observation. Its policy changes which experiences it collects. This feedback loop distinguishes the problem from a fixed supervised dataset.","A Markov decision process specifies states, actions, transition probabilities, rewards, and a discount factor. The Markov assumption means the state contains the information needed to predict the next transition given the action. An observation may be incomplete; a recurrent policy can use history, but recurrence does not automatically make every hidden process fully observable."],formula:"sₜ → action aₜ ~ π(·|sₜ) → reward rₜ, next state sₜ₊₁\nReturn Gₜ = rₜ + γrₜ₊₁ + γ²rₜ₊₂ + …"},
 {id:"values",title:"Predict future return under a policy",paragraphs:["Vπ(s) is expected discounted return starting in state s and following policy π. Qπ(s,a) conditions additionally on the first action. Reward is immediate feedback; value estimates the longer-term consequences.","The Bellman expectation equation relates a value to immediate reward plus the value of the next state under the policy. The optimality equation instead maximizes over the next action. That maximum changes the learning target; it is not merely another notation for evaluating a fixed policy."],formula:"Vπ(s) = Eπ[r + γVπ(s′) | s]\nQ*(s,a) = E[r + γ maxa′ Q*(s′,a′) | s,a]",worked:{title:"A discounted three-reward return",steps:["With rewards [0,1,2] and γ=0.9, G=0+0.9×1+0.9²×2=2.52.","The immediate reward at the first step is zero even though the return is positive.","Changing the discount changes the tradeoff between earlier and later rewards; it does not repair a badly chosen reward definition."]}},
 {id:"td",title:"Learn from a temporal-difference error",paragraphs:["Tabular Q-learning moves one state–action entry toward a bootstrapped target. The temporal-difference error compares that target with the current estimate. Bootstrapping uses a model's own estimate of future return, so targets can be biased or unstable.","Epsilon-greedy exploration chooses a random action with probability ε and otherwise selects a current maximizer. The random branch can also happen to choose the greedy action. Exploration changes what the agent learns about, not just how noisy its outputs look."],formula:"y = r + γ maxa′ Q(s′,a′) for a nonterminal transition\nδ = y − Q(s,a)\nQ(s,a) ← Q(s,a) + ηδ",worked:{title:"One update",steps:["Let r=1, γ=0.9, max Q(next)=2, and current Q=0.5. The nonterminal target is 2.8.","The TD error is 2.3. At learning rate 0.1, the updated table entry is 0.73.","For a true terminal transition the target is only 1, giving an updated entry of 0.55."]},lab:"bellman"},
 {id:"dqn",title:"Replace the table with a neural value function",paragraphs:["A DQN maps a state to one estimated Q-value per discrete action. These are return estimates, not class probabilities, so the output normally has no softmax. Training gathers the estimate for the action actually taken and regresses it toward a detached target.","Experience replay stores transitions and samples batches to reduce immediate temporal correlation and reuse experience. A target network changes more slowly than the online network, reducing how rapidly the regression target moves. Replay does not make experiences perfectly independent or eliminate distribution shift."],code:"q_selected = online(states).gather(1, actions[:,None]).squeeze(1)\nwith torch.no_grad():\n    next_value = target(next_states).max(dim=1).values\n    y = rewards + gamma * (~terminated).float() * next_value\nloss = F.smooth_l1_loss(q_selected, y)\n# Periodically: target.load_state_dict(online.state_dict())"},
 {id:"termination",title:"Episode ending is not always task termination",paragraphs:["A terminal state ends future return, so its target has no bootstrap term. A time-limit truncation of a continuing task stops data collection without necessarily making future value zero. Conflating them biases value estimates.","Bootstrap from the actual final observation when a rollout is truncated, not from the next episode's reset observation. If a finite horizon is intrinsic to the task, remaining time belongs in the state and horizon completion can be terminal. Define the task semantics before coding a generic done flag."],table:{headers:["Event","Bootstrap?","Reason"],rows:[["Goal or true terminal failure","No","No future return in that episode's task"],["External time limit on a continuing task","Yes, from final observation","The underlying process could continue"],["Intrinsic finite-horizon completion","No","The task definition ends, with time represented in state"]]}},
 {id:"policy-gradient",title:"Learn the policy directly",paragraphs:["Policy-gradient methods adjust action probabilities in proportion to their estimated consequences. REINFORCE uses sampled returns to weight log-probability gradients. A baseline independent of the selected action can reduce variance without changing the expected score-function gradient under the usual assumptions.","Actor–critic methods learn a policy (actor) and a value estimate (critic). An advantage estimate compares an action's outcome with a baseline, often using a bootstrapped TD estimate. Bootstrapping can reduce variance while introducing estimation bias. Old trajectories cannot be reused indiscriminately in an on-policy objective; off-policy corrections or a suitable off-policy algorithm are needed."],formula:"∇θ J ≈ mean[∇θ log πθ(aₜ|sₜ) · (Gₜ − b(sₜ))]\nActor–critic TD advantage: Âₜ = rₜ + γV(sₜ₊₁) − V(sₜ)\nAt true termination, remove the next-value term."},
 {id:"evaluation",title:"Evaluate behavior, not just the TD loss",paragraphs:["A smaller TD loss can mean the network fits its current targets without solving the task well. Evaluate returns, success rates, episode lengths, and actual trajectories using a declared evaluation policy. Separate exploration during training from greedy or otherwise specified evaluation behavior.","Repeat independent training seeds and use held-out environment initializations or variations appropriate to the generalization claim. Record environment steps, not only optimizer updates. A reward proxy can be exploited in unintended ways, so inspect behavior against the real objective rather than celebrating reward alone."]},
 {id:"capstone",title:"Connect the full deep learning course",paragraphs:["Choose a problem where you can state the available observations, target or reward, inference constraints, and evaluation protocol. Compare two justified approaches against a simple baseline. Use a controlled ablation to test why a proposed mechanism helps.","For a vision problem, compare convolutional and residual features; for sequences, recurrence and attention; for generation, a VAE, GAN, or diffusion model; for control, a value-based or policy-based method. You do not need every architecture in one project. Match the mechanism to the problem.","Deliver runnable code, fixed splits or environment settings, validation decisions, final metrics, cost, failure cases, and a model card. Explain one result that challenged your expectation. The end of the course is the ability to defend a modeling decision with evidence."],table:{headers:["Deliverable","Evidence to include"],rows:[["Problem and baseline","What is predicted or optimized, and what simple method establishes a floor"],["Controlled comparison","Data access, tuning budget, seeds, and an isolating ablation"],["Final evaluation","Held-out performance, appropriate uncertainty, and cost"],["Reliability report","Stress tests, limitations, intended use, and reproducibility instructions"]]}}
 ],pitfalls:[["Softmax on Q-values","Q-values estimate returns; they are not required to sum to one."],["Training through the target","Detach the bootstrapped target and update the online estimate for the observed action."],["All episode endings mean zero future value","Distinguish task termination from external truncation and use the true final observation."],["High reward proves the desired behavior","Inspect trajectories and confirm the reward has not encouraged an unintended shortcut."]],
 practice:[["Update Q=2 with r=−1, γ=0.95, max Q(next)=4, and η=0.2.","The nonterminal target is −1+0.95×4=2.8. The error is 0.8 and new Q=2.16. If terminal, target=−1 and new Q=1.4."],["With ε=0.2 and two actions, how often is the unique greedy action selected?","The greedy branch contributes 0.8 and uniform exploration contributes 0.2×0.5=0.1, totaling 0.9."],["What should a replay transition store?","At least state, action, reward, actual next state, and termination semantics. Store truncation separately when it affects rollout handling or analysis."],["Design a control evaluation.","Declare policy randomness, use multiple independent training seeds, evaluate returns and success over held-out starts, report environment-step budget, and inspect trajectories for reward exploitation."]],
 review:[["How is reward different from return?","Reward is immediate feedback; return aggregates future rewards with the stated discounting."],["What makes a state Markov?","It contains sufficient information for the next-state/reward distribution given the action."],["What does Q(s,a) estimate?","Expected future discounted return after taking action a in state s under the specified continuation policy."],["Why use replay?","To reuse collected experience and reduce short-range correlation in update batches."],["Why use a target network?","To slow changes in bootstrapped regression targets."],["When is bootstrapping removed?","At true task termination, not automatically at an external time limit."],["What does a baseline do in policy gradients?","It can reduce variance while preserving the expected gradient when chosen with the required action independence."],["Why is TD loss insufficient as a success measure?","It evaluates agreement with current value targets, not necessarily useful behavior or high realized returns."]],
 experiment:{title:"A complete DQN in a small line world",description:"Train a DQN agent to reach the right-hand goal in a seven-position world. Replay, target copies, epsilon-greedy exploration, detached targets, and separate termination/truncation handling are implemented directly. The script compares greedy evaluation with a random policy across all nonterminal starting positions.",checks:["Verify the terminal target equals the immediate reward.","Confirm time-limit transitions store their true final state and still bootstrap.","Compare success rate and undiscounted return, not just TD loss.","Repeat independent seeds before claiming reliable learning, and inspect the printed greedy path."]},bridge:"You have reached the end of the 14-week path: from a first neuron to representations, generation, reliability, and learning through action. Return to the map to revisit the mechanism your next experiment needs.",references:[{title:"Mnih et al. — Human-level control through deep reinforcement learning",url:"https://doi.org/10.1038/nature14236",note:"Deep Q-networks, replay, and target-network stabilization."},{title:"Gymnasium — Handling Time Limits",url:"https://gymnasium.farama.org/tutorials/gymnasium_basics/handling_time_limits/",note:"Correct bootstrapping at termination versus truncation."},{title:"Sutton & Barto — Reinforcement Learning: An Introduction",url:"http://incompleteideas.net/book/the-book-2nd.html",note:"Foundations of values, temporal-difference learning, and policy gradients."}]
};
