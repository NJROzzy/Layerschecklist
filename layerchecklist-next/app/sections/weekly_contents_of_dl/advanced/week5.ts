import type { CourseLessonData } from "./types";
export const week5: CourseLessonData = {
 week:5,title:"Residual Networks and Transfer Learning",lead:"Depth gives a model more opportunities to transform a representation. Residual paths make those transformations easier to optimize; transfer learning lets us begin with features learned from another task.",prerequisites:"CNN tensor shapes, backpropagation, overfitting, and validation-based model selection.",
 outcomes:["Distinguish optimization degradation from overfitting.","Derive the residual gradient path and implement a projection shortcut.","Choose between frozen features and fine-tuning with an explicit BatchNorm policy.","Compare transfer and training from scratch without hiding their different data budgets."],
 sections:[
 {id:"degradation",title:"When a deeper network trains worse",paragraphs:["A deeper model may have enough capacity to represent a shallower model's solution yet fail to find it with the chosen training procedure. If both training and validation error rise with depth, suspect an optimization problem before blaming overfitting.","Overfitting has a different signature: training improves while held-out performance deteriorates. These explanations can coexist. Compare curves under controlled initialization, learning-rate tuning, and training budgets rather than diagnosing from the final validation score alone."]},
 {id:"residual-path",title:"Learn a correction to the current representation",paragraphs:["A residual block outputs y = x + F(x). If preserving the input is useful, the residual branch can learn a small correction instead of reconstructing the identity through every nonlinear layer. This changes the optimization problem without preventing substantial transformations.","Differentiating the addition gives an identity term alongside the residual branch's Jacobian. That provides a direct route for a gradient, but it is not an unconditional guarantee: the branch can cancel the identity, and activations after the sum can block components. In a vector network, matrix products and directions matter, not just a single scalar multiplier."],formula:"y = x + F(x; θ)\n∂y/∂x = I + JF(x)\n∇x L = (I + JF(x))ᵀ ∇y L",worked:{title:"A scalar stack isolates the identity path",steps:["Let F(x) = a·x and stack L identical linear blocks. A plain stack has derivative aᴸ; the residual stack has derivative (1+a)ᴸ.","At a = 0.1 and L = 10, the plain derivative is 10⁻¹⁰, while the residual derivative is approximately 2.594.","At a = −1 the residual derivative is zero. At a = 0.5, repeated residual multiplication can explode. The shortcut helps access identity; it does not solve every conditioning problem."]},lab:"residual"},
 {id:"projection",title:"Make both branches agree on shape",paragraphs:["Addition requires compatible tensors. An identity shortcut works when channels and spatial dimensions are unchanged. If a block downsamples or expands channels, use a projection on the shortcut, commonly a 1 × 1 convolution with the required stride.","A projection is learned and is no longer a literal identity. Track both branches explicitly. Broadcasting a mistaken shape may run without representing the architecture you intended."],formula:"Input: [N, 64, 56, 56]\nResidual: 3×3, stride 2, 64→128 → [N, 128, 28, 28]\nShortcut: 1×1, stride 2, 64→128 → [N, 128, 28, 28]\nSum: [N, 128, 28, 28]",code:"shortcut = nn.Conv2d(64, 128, 1, stride=2, bias=False)\nbranch = nn.Sequential(\n    nn.Conv2d(64, 128, 3, stride=2, padding=1, bias=False),\n    nn.BatchNorm2d(128), nn.ReLU(),\n    nn.Conv2d(128, 128, 3, padding=1, bias=False),\n    nn.BatchNorm2d(128),\n)\ny = F.relu(branch(x) + shortcut(x))"},
 {id:"normalization",title:"Activation order and BatchNorm state",paragraphs:["A post-activation block applies an activation after adding the branches. A pre-activation design moves normalization and activation before the convolutions and can leave the addition path unobstructed by a final ReLU. Treat these as distinct designs; moving layers changes the function and gradient routes.","BatchNorm has both trainable affine parameters and running statistics. Setting requires_grad=False freezes parameter gradients but does not stop running mean and variance updates in training mode. Conversely, eval() changes BatchNorm and dropout behavior but does not disable autograd. A frozen feature extractor normally needs an explicit evaluation-mode policy."]},
 {id:"transfer",title:"Reuse features, then choose what may adapt",paragraphs:["Pretraining learns parameters on a source task or dataset. Transfer learning reuses some of those parameters for a target task. Earlier visual features often transfer more broadly than a source-specific classifier, but domain and acquisition differences can make that intuition fail.","Feature extraction freezes the backbone and trains a replacement head. Fine-tuning updates selected backbone layers or the entire model. The useful choice depends on label count, domain similarity, compute, and validation evidence. Replacing an RGB input with grayscale or changing normalization and resolution also changes the inputs the pretrained filters receive."],table:{headers:["Target situation","Starting hypothesis","Evidence to collect"],rows:[["Few labels, similar imagery","Frozen backbone + new head","Probe performance and overfitting gap"],["More labels, similar imagery","Fine-tune upper blocks first","Validation gains versus added cost"],["Different acquisition or domain","Compare partial/full adaptation and scratch","Whether source features cause negative transfer"],["Tiny dataset, large domain shift","Conservative tuning and strong baselines","Uncertainty across splits or seeds"]]},takeaway:"A transferred model has benefited from source data. Record that advantage instead of treating it as the same data budget as training from scratch."},
 {id:"fine-tuning",title:"Implement an explicit adaptation policy",paragraphs:["First train a newly initialized head while holding the backbone fixed. If validation supports further adaptation, unfreeze selected layers and use a smaller learning rate there. Differential rates are a practical hypothesis, not a universal optimum.","An optimizer only updates the parameters assigned to it. Rebuild or extend parameter groups when unfreezing. During every training epoch, model.train() can recursively put a frozen backbone's BatchNorm layers back into training mode; restore the intended backbone mode afterwards."],code:"for p in backbone.parameters():\n    p.requires_grad_(False)\nbackbone.eval()\nhead.train()\nwith torch.no_grad():\n    features = backbone(images)\nloss = F.cross_entropy(head(features), labels)\n\n# Later: fine-tuning, with fresh optimizer groups.\nfor p in backbone.parameters():\n    p.requires_grad_(True)\noptimizer = torch.optim.AdamW([\n    {'params': backbone.parameters(), 'lr': 1e-4},\n    {'params': head.parameters(), 'lr': 1e-3},\n])"},
 {id:"cv-study",title:"A defensible computer-vision study",paragraphs:["Compare a small CNN, a residual model, and a transfer strategy using a declared validation budget. Apply the same target split and evaluation preprocessing. Report source pretraining, trainable and total parameter counts, runtime, and error types, not only the winning accuracy.","Augmentations must preserve task labels. A horizontal flip may be sensible for some object categories but invalid for text direction or medical laterality. Strong cropping can remove the labeled object, and color changes can erase the signal in a color-dependent task. Inspect actual augmented examples before training.","Add an ablation that isolates your explanation: remove the shortcut while holding widths fixed, compare a frozen backbone with fine-tuning, or vary one augmentation at a time. A larger model earning a better score does not by itself explain why."]}
 ],
 pitfalls:[["The shortcut fixes all gradients","Identity routes help, but cancellation, scaling, normalization, and nonlinear gates still affect gradient behavior."],["Frozen parameters imply frozen BatchNorm","Running statistics are buffers updated by training-mode forwards. Set the mode intentionally."],["An unfreezing change has no effect","Check optimizer parameter groups and actual gradient norms, not only requires_grad flags."],["Transfer must improve the result","Source features can be a poor match. Keep a scratch baseline and report negative transfer when observed."]],
 practice:[["Project [N,32,28,28] to [N,64,14,14]. How many shortcut weights are needed?","Use Conv2d(32,64,1,stride=2,bias=False). It has 32×64 = 2,048 weights. A 3×3 stride-2 padded residual branch produces the matching shape."],["A deeper model has higher training loss. What should you try first?","Check implementation, learning-rate behavior, gradient statistics, and optimization budget. Add a residual comparison with matched widths. More regularization is not the first explanation for inability to fit training data."],["Why do frozen features change between epochs?","A BatchNorm backbone may still be in training mode, or stochastic transforms/dropout may alter its inputs or outputs. Freezing parameters alone does not define a deterministic feature pipeline."],["Write a transfer result you would trust.","State source data and checkpoint, target splits, input preprocessing, frozen layers, BatchNorm policy, tuning budget, validation selection, and final held-out performance. Include a scratch baseline and compute costs."]],
 review:[["What is the residual function learning?","A correction F(x) to the shortcut representation, with the sum representing the desired transformation."],["Can a residual block represent identity?","With a shape-preserving identity shortcut and zero residual branch, the sum is x; a post-sum activation can still modify negative coordinates."],["Why use a 1×1 projection?","It mixes channels and can subsample with stride to make the shortcut's dimensions match the residual branch."],["What distinguishes eval() from no_grad()?","eval() changes mode-dependent layers. no_grad() disables recording operations for gradient computation. They serve different purposes."],["Why give the new head a larger learning rate?","It begins without task-specific weights, while pretrained features may need smaller adjustments. Validate the choice."],["What is negative transfer?","Reusing source training hurts the target outcome relative to an appropriate baseline under the stated evaluation protocol."],["What makes a residual ablation convincing?","It isolates the shortcut while controlling other architectural and training differences, then examines both training and held-out behavior."],["When should augmentation be rejected?","When it changes the label, removes essential evidence, or creates unrealistic examples that undermine the target task."]],
 experiment:{title:"Transfer inside a small residual network",description:"Pretrain a tiny residual backbone on clean synthetic stripe orientations, then compare a frozen head, fine-tuning, and training from scratch on a smaller noisy target dataset. Pretraining happens locally; no external checkpoint is downloaded. The script restores the best validation checkpoint separately for each strategy.",checks:["Confirm the residual downsampling branch and projection produce identical shapes.","Compare total and trainable parameters for the frozen and fine-tuned cases.","Check that frozen-backbone buffers remain unchanged during target training.","Interpret the three test results as one small study, not a guaranteed ranking. Repeat seeds and vary the domain shift."]},bridge:"Residual learning improves the path through depth. Sequence models introduce another repeated path: through time. Next we study how information survives—or disappears—along it.",references:[{title:"He et al. — Deep Residual Learning for Image Recognition",url:"https://arxiv.org/abs/1512.03385",note:"The original residual-learning formulation and image-recognition experiments."},{title:"He et al. — Identity Mappings in Deep Residual Networks",url:"https://arxiv.org/abs/1603.05027",note:"Identity paths and pre-activation residual designs."},{title:"PyTorch — Transfer Learning Tutorial",url:"https://docs.pytorch.org/tutorials/beginner/transfer_learning_tutorial.html",note:"Feature extraction, fine-tuning, and practical training mechanics."}]
};
