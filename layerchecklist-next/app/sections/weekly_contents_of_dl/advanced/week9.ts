import type { CourseLessonData } from "./types";
export const week9: CourseLessonData = {
 week:9,title:"Autoencoders and Variational Autoencoders",lead:"Reconstruction teaches an encoder what to preserve. A variational autoencoder goes further: it learns a distribution over latent codes and connects those codes to a prior we can sample.",prerequisites:"Encoders, probability distributions, gradients, reconstruction losses, and expectations.",outcomes:["Distinguish deterministic reconstruction from generative modeling.","Derive and calculate Gaussian reparameterization and KL divergence.","Interpret the ELBO and the reconstruction–regularization tradeoff.","Compare reconstructions, prior samples, and latent-use diagnostics."],
 sections:[
 {id:"autoencoder",title:"Compress, then reconstruct",paragraphs:["An autoencoder maps x to a code z and decodes that code to a reconstruction. A bottleneck, sparsity, corruption, or other constraint makes copying less trivial. A high-capacity unconstrained model can approximate identity without learning the representation we hoped for.","Reconstruction emphasizes information needed by its loss. A feature important for classification may contribute little to pixel error; nuisance detail may contribute a lot. A linear autoencoder with suitable squared-error conditions connects to PCA subspaces, but arbitrary nonlinear autoencoders do not inherit every PCA property."],formula:"z = fφ(x), x̂ = gθ(z)\nLreconstruction = mean over examples of a per-example distortion"},
 {id:"likelihood",title:"A loss is an assumption about observations",paragraphs:["Squared error corresponds, up to constants and scaling, to a Gaussian observation model with fixed variance. Binary cross-entropy corresponds to a Bernoulli observation model. Rescaling arbitrary continuous pixels into [0,1] does not by itself make them Bernoulli observations.","A denoising autoencoder receives corrupted input but reconstructs a clean target. It learns a mapping tied to that corruption process. This can discourage literal copying, although success on one noise type does not establish robustness to every shift."],table:{headers:["Choice","Interpretation","Caution"],rows:[["Squared error","Fixed-variance Gaussian reconstruction","Variance and reduction set the scale."],["Binary cross-entropy","Bernoulli observation likelihood","Continuous normalization is not a Bernoulli justification."],["Denoising input","Recover clean structure from corruption","The corruption must match the learning goal."]]}},
 {id:"latent-distribution",title:"Why an ordinary autoencoder is not enough for sampling",paragraphs:["A deterministic encoder can place training codes in disconnected or irregular regions. Drawing a random point between or beyond those codes may visit areas the decoder never learned. Reconstruction alone supplies neither a useful sampling prior nor a complete probabilistic generative model.","A VAE defines a prior p(z), a decoder likelihood pθ(x|z), and an approximate posterior qφ(z|x). Commonly p(z) is standard normal and q is diagonal Gaussian, with the encoder producing a mean and log-variance for each latent coordinate."],formula:"p(z) = N(0,I)\nqφ(z|x) = N(μ(x), diag(exp(logvar(x))))\npθ(x) = ∫ pθ(x|z) p(z) dz"},
 {id:"reparameterization",title:"Move randomness outside the learned parameters",paragraphs:["Sampling z directly from a parameterized Gaussian obscures the simple path from parameters to the sample. Reparameterization draws independent standard noise ε, then constructs z as a differentiable function of μ and σ. Randomness remains; gradients can now flow through the transformation.","The network predicts log-variance for numerical convenience. Standard deviation is exp(0.5·logvar), not exp(logvar). Large log-variances can still cause unstable exponentials, so inspect their values and the training objective."],formula:"ε ~ N(0,I)\nσ = exp(0.5·logvar)\nz = μ + σ ⊙ ε",code:"mu, logvar = encoder(x).chunk(2, dim=-1)\nstd = torch.exp(0.5 * logvar)\nz = mu + std * torch.randn_like(std)\nreconstruction = decoder(z)"},
 {id:"elbo",title:"Balance reconstruction and prior agreement",paragraphs:["The evidence lower bound is expected log-likelihood minus the posterior-to-prior KL divergence. Maximizing it encourages plausible reconstruction while limiting how far each approximate posterior departs from the prior. Its negative is a reconstruction negative log-likelihood plus KL.","For diagonal Gaussian q and standard-normal p, KL is available analytically. Sum over latent dimensions, then average over examples. Similarly, state how reconstruction is reduced over input features. Changing one reduction silently changes the relative weight. β=1 gives the ordinary negative ELBO; other β values define a modified objective."],formula:"ELBO = Eq[log pθ(x|z)] − KL(qφ(z|x) || p(z))\nKL = ½ Σj [μj² + exp(logvarj) − 1 − logvarj]\nLβ = reconstruction NLL + β·KL",worked:{title:"One latent dimension",steps:["Let μ=1 and logvar=0, so σ²=1.","KL = ½(1²+1−1−0) = 0.5 nats.","At μ=0 and logvar=0, q equals the standard-normal prior and KL is zero.","With reconstruction NLL 2 and β=0.2, the modified objective is 2.1. This is not the ordinary β=1 negative ELBO."]},lab:"vae"},
 {id:"sampling",title:"Separate three different questions",paragraphs:["Reconstruction asks whether the model can recover an observation from its inferred code. Prior sampling asks whether z drawn from p(z) decodes into plausible new observations. Interpolation asks what the decoder does along a chosen latent path. These are different tests.","A decoder may output the mean of pθ(x|z). Displaying that mean is different from sampling the observation distribution itself. State which you show. Inspect distribution coverage as well as individual attractive examples; a smooth interpolation does not prove the learned marginal matches the data."],table:{headers:["Test","Latent source","What it probes"],rows:[["Reconstruction","q(z|observed x)","Encoding and decoding observed structure"],["Prior sample","p(z)","Generative behavior away from selected examples"],["Interpolation","A path between selected codes","Decoder behavior along that path"]]}},
 {id:"collapse",title:"Detect when the decoder ignores the latent",paragraphs:["Posterior collapse occurs when the latent conveys little useful information and the decoder relies mostly on its own capacity. Small KL can be a symptom, but KL alone is not sufficient proof. Compare reconstructions after replacing or shuffling codes, inspect active dimensions, and check conditional variation.","A larger KL is not automatically better either. It may reflect poor prior agreement without improving useful information. Evaluate reconstruction, prior samples, and latent interventions together. β schedules, architecture changes, and capacity constraints are hypotheses to validate, not guaranteed fixes."]}
 ],pitfalls:[["Using log-variance as standard deviation","Convert with exp(0.5·logvar) before multiplying noise."],["A hidden reduction changed β","Record whether losses sum or average over features and latent dimensions."],["Good reconstructions imply good prior samples","The encoder can use code regions rarely reached by the prior."],["Small KL proves collapse","Check whether changing the latent changes decoded predictions, alongside variation and reconstruction diagnostics."]],
 practice:[["Calculate KL for μ=0 and variance 4 in one dimension.","logvar=log(4). KL=½(4−1−log4)≈0.8069 nats."],["For μ=1, σ=2, ε=−0.5, what is z?","z=1+2×(−0.5)=0. The random variable still has variance 4 across repeated draws."],["Why does averaging reconstruction over 100 features change the objective?","It shrinks reconstruction by a factor of 100 relative to a summed reconstruction, unless the KL coefficient is adjusted correspondingly."],["Test whether a decoder uses z.","Hold observations fixed, compare ordinary codes with shuffled or replaced codes, and measure reconstruction changes. Inspect per-dimension activity and prior samples rather than using KL alone."]],
 review:[["What prevents a trivial autoencoder identity?","A constraint such as bottleneck capacity, corruption, sparsity, or regularization; architecture and data still matter."],["What does the VAE encoder output?","Parameters of an approximate posterior, commonly mean and log-variance."],["Does reparameterization remove randomness?","No. It expresses randomness through parameter-independent noise and a differentiable transformation."],["What is the prior used for?","It supplies a latent distribution from which new samples can be generated."],["Which way does the KL point?","The ordinary VAE uses KL(qφ(z|x) || p(z))."],["When is β-weighting the ordinary ELBO?","At β=1 with a correctly defined reconstruction negative log-likelihood."],["Is a decoder mean an observation sample?","No. Sampling p(x|z) includes the observation distribution's variability."],["Why inspect more than reconstruction loss?","It does not alone establish latent use, prior-sample quality, or distribution coverage."]],
 experiment:{title:"A VAE on a ring of small clusters",description:"Train a two-dimensional latent VAE on a synthetic mixture. The script uses a fixed-variance Gaussian decoder, explicit feature-summed negative log-likelihood and latent-summed KL, validation selection, and held-out reconstruction. It exports decoder means from prior draws to CSV.",checks:["Inspect reconstruction and KL separately; a lower combined objective can hide a tradeoff.","Plot the exported prior decoder means and compare their mode coverage with the data.","Compare reconstruction using inferred means versus shuffled latent means.","Vary β while keeping reductions, splits, and training budget unchanged."]},bridge:"A VAE trains through a probabilistic reconstruction bound. Next we learn to generate by competing against a discriminator, with a different set of optimization challenges.",references:[{title:"Kingma & Welling — Auto-Encoding Variational Bayes",url:"https://arxiv.org/abs/1312.6114",note:"Variational inference, reparameterization, and the VAE objective."},{title:"PyTorch — Normal distribution",url:"https://docs.pytorch.org/docs/stable/distributions.html#normal",note:"Normal likelihoods and reparameterized sampling conventions."}]
};
