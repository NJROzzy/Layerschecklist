import type { CourseLessonData } from "./types";
export const week7: CourseLessonData = {
 week:7,title:"Attention and Transformers",lead:"Let each position retrieve the information relevant to it. Calculate attention by hand, add position and masking, then assemble a transformer that respects its information boundary.",prerequisites:"Dot products, softmax, sequence modeling, residual paths, and tensor dimensions.",outcomes:["Calculate scaled dot-product attention with masks.","Track multi-head tensor shapes and explain position information.","Compare encoder, encoder–decoder, and decoder-only models.","Train next-token prediction and test for future-token leakage."],
 sections:[
 {id:"retrieval",title:"Queries, keys, and values",paragraphs:["A query describes what a position seeks, a key provides a matching representation, and a value carries retrievable information. They are learned projections rather than hand-written tags. Query–key dot products become scores; row-wise softmax turns them into distributions over source keys.","The weighted value sum has a fixed feature width even when source length changes. Cross-attention can use different query and source lengths, allowing each decoder position to retrieve from all permitted encoder positions."],formula:"Q = Xq WQ; K = Xk WK; V = Xk WV\nA = softmax(QKᵀ / √dk + mask), along keys\nOutput = A V",table:{headers:["Tensor","One-head shape","Role"],rows:[["Q","Tq × dk","Destination queries"],["K","Tk × dk","Source keys"],["V","Tk × dv","Source content"],["A","Tq × Tk","One distribution per query"],["Output","Tq × dv","Retrieved content"]]}},
 {id:"softmax",title:"Calculate an attention read",paragraphs:["Subtracting the maximum score before exponentiation improves numerical stability without changing softmax. Scaling dot products by √dk moderates their magnitude under common variance assumptions. It does not replace all normalization or initialization choices.","The correct softmax of [2,0.5,−1] is approximately [0.7856,0.1753,0.0391]. Normalize over the keys for each query, and check that masked keys receive zero. The lab's adjustable temperature illustrates sharpness separately from the standard dimension scaling."],worked:{title:"Scores [2,0.5,−1], values [1,3,−2]",steps:["Subtract 2: [0,−1.5,−3]. Exponentiate: [1,0.22313,0.04979].","Divide by 1.27292 to obtain [0.78560,0.17529,0.03911].","The output is 0.78560×1 + 0.17529×3 − 0.03911×2 ≈ 1.23324."]},lab:"attention"},
 {id:"positions",title:"Self-attention, cross-attention, and position",paragraphs:["Self-attention derives Q, K, and V from the same sequence. Cross-attention gets Q from one sequence and K/V from another. The matrix operation is the same; the information sources differ.","Unmasked content-only self-attention is permutation equivariant: reordering the inputs reorders the outputs. Learned absolute embeddings, sinusoidal encodings, or relative-position mechanisms can supply order information. A causal mask also introduces an ordering constraint."],formula:"Original sinusoidal positions:\nPE(pos,2i) = sin(pos / 10000^(2i/dmodel))\nPE(pos,2i+1) = cos(pos / 10000^(2i/dmodel))"},
 {id:"heads",title:"Several retrieval spaces inside one block",paragraphs:["Each attention head learns its own projections. Head outputs are concatenated and projected to model width. Heads can specialize, but they are not guaranteed to represent clean human categories.","The block also includes a position-wise feed-forward network, residual paths, and normalization. Attention mixes positions; the feed-forward network transforms each position's features. Pre-norm and post-norm orderings change the computation and its gradient routes."],formula:"headᵢ = Attention(XWQᵢ, XWKᵢ, XWVᵢ)\nMultiHead(X) = concat(head₁,…,headₕ) WO\ndmodel=64, heads=4 → 16 features per head",code:"mha = nn.MultiheadAttention(64, 4, batch_first=True)\nout, weights = mha(x,x,x, average_attn_weights=False)\n# x, out: [N,T,64]; weights: [N,4,T,T]"},
 {id:"families",title:"Choose which information is available",paragraphs:["An encoder uses bidirectional context within an observed input. An encoder–decoder adds a causal output stream that cross-attends to the encoder. A decoder-only model predicts using earlier context without requiring a separate encoder.","The architecture establishes what a prediction may observe. Define the inference boundary before selecting masks. A model intended for online use cannot rely on future observations."],table:{headers:["Family","Available context","Typical objective"],rows:[["Encoder","All permitted input positions","Classification or masked prediction"],["Encoder–decoder","Observed input plus earlier output","Conditional generation"],["Decoder-only","Current and earlier context","Next-token prediction"]]}},
 {id:"causality",title:"Block future tokens before softmax",paragraphs:["For next-token training, supervise each input position with the following token. A causal mask prevents the model from reading later input positions. Shifting targets without masking still permits leakage.","Boolean conventions differ by API. In MultiheadAttention and TransformerEncoder masks, True means blocked. In scaled_dot_product_attention, True means allowed. Padding masks address nonexistent positions; causal masks address future positions. Both may be needed."],code:"inputs, targets = tokens[:, :-1], tokens[:, 1:]\nT = inputs.size(1)\nblocked = torch.triu(torch.ones(T,T,dtype=torch.bool), diagonal=1)\nfeatures = encoder(embedding(inputs) + positions[:T], mask=blocked)\nlogits = head(features)\nloss = F.cross_entropy(logits.reshape(-1, vocab), targets.reshape(-1))",takeaway:"Change a sequence suffix in evaluation mode. Earlier logits must remain unchanged in a causal model."},
 {id:"cost",title:"Parallel training, sequential generation",paragraphs:["Teacher-forced training can evaluate known positions in parallel behind a causal mask. Free generation remains sequential because each newly generated token becomes context for the next. Key/value caching reduces repeated work but does not remove that dependency.","Dense attention performs quadratic pairwise score work as sequence length grows. A naïve implementation also stores a quadratic score matrix; optimized kernels can avoid fully materializing it. Separate arithmetic complexity from actual memory behavior.","A fair recurrent-versus-transformer study fixes splits, input information, and tuning budgets, then reports accuracy and measured cost. Attention maps are diagnostics, not automatic causal explanations: values, residual streams, and later transformations also affect predictions."]}
 ],pitfalls:[["Normalizing the wrong axis","Normalize over source keys independently for each query."],["A shifted target prevents all leakage","The attention mask must also block later inputs."],["Copying a boolean mask between APIs","Check the called function's semantics; True does not always mean blocked."],["Attention weight equals explanation","Test interventions and alternative paths before attributing causal importance."]],
 practice:[["Mask the final key in the three-score worked example.","The remaining weights are [0.81757,0.18243,0], and the scalar output is 1.36485."],["Give the weight shape for N=8, T=20, four heads.","Per-head weights are [8,4,20,20]. Head-averaged weights are [8,20,20]. At model width 64 the output is [8,20,64]."],["Design a causality assertion.","Disable dropout, change only a suffix, and compare earlier logits with a small numerical tolerance. The supplied script runs this before training."],["What happens when context length doubles?","Dense score interactions quadruple. Position-wise feed-forward work approximately doubles at fixed width. Actual runtime also depends on kernels and hardware."]],
 review:[["Why separate keys and values?","Matching and retrieved content can live in different learned spaces."],["Why divide by √dk?","Dot-product variance commonly grows with dimension; scaling moderates softmax saturation."],["When is the mask applied?","Before softmax, so forbidden entries receive zero probability."],["Does content-only attention know absolute order?","No; position mechanisms or structural constraints must provide order information."],["What does the feed-forward sublayer do?","It applies a shared nonlinear feature transformation separately at each position."],["Is decoder training sequential across tokens?","Known training positions can be parallelized with a causal mask; free generation is sequential."],["Are attention heads guaranteed interpretable roles?","No. Specialization is learned and may be distributed or redundant."],["What controls a fair comparison with RNNs?","Data, available context, tokenization, tuning budget, and deployment constraints."]],
 experiment:{title:"Train a tiny causal transformer",description:"Learn modular counting sequences using positional embeddings, two causal transformer blocks, shifted targets, and validation checkpoint selection. A future-token perturbation assertion runs before training. The final report includes reserved test loss and generated tokens.",checks:["The causality assertion must pass.","Compare test cross-entropy with the uniform baseline log(vocabulary size). This is a toy sequence task, not a language benchmark.","Check generated tokens for the learned increment pattern.","Ablate position information while keeping the rest of the experiment fixed."]},bridge:"Attention is an architecture. Next we explore training targets constructed from the data itself, enabling representation learning before task-specific labels.",references:[{title:"Vaswani et al. — Attention Is All You Need",url:"https://arxiv.org/abs/1706.03762",note:"Scaled attention, heads, positions, and the transformer architecture."},{title:"PyTorch — MultiheadAttention",url:"https://docs.pytorch.org/docs/stable/generated/torch.nn.MultiheadAttention.html",note:"Mask semantics and returned weight shapes."},{title:"PyTorch — scaled_dot_product_attention",url:"https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.scaled_dot_product_attention.html",note:"The lower-level function's distinct boolean mask convention."}]
};
