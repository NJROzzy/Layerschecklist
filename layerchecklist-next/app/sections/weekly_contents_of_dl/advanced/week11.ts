import type { CourseLessonData } from "./types";
export const week11: CourseLessonData = {
 week:11,title:"Diffusion Models",lead:"Destroy structure in controlled amounts, learn what noise was added, and use that knowledge to build a reverse sampling process. A simple regression target leads to an iterative generative model.",prerequisites:"Gaussian noise, expectations, neural regression, and the generative questions from VAEs and GANs.",outcomes:["Calculate the forward process directly at any time step.","Train a time-conditioned noise predictor with correct batch broadcasting.","Distinguish an oracle reconstruction from a learned reverse process.","Implement a complete DDPM sampler and explain conditioning tradeoffs."],
 sections:[
 {id:"forward",title:"A known process gradually removes structure",paragraphs:["Choose a variance schedule β₁,…,βT and define αt=1−βt. Each forward step shrinks the previous signal and adds Gaussian noise. Multiplying α values gives ᾱt, the retained signal power relative to the original observation.","The Gaussian composition permits sampling a noisy observation directly at any t without simulating every earlier step. This is why training can sample independent time steps efficiently. A terminal distribution close to standard normal requires ᾱT to be sufficiently small for the data scale."],formula:"xₜ = √αₜ xₜ₋₁ + √βₜ εₜ\nᾱₜ = ∏s=1…t αs\nxₜ = √ᾱₜ x₀ + √(1−ᾱₜ) ε, ε~N(0,I)",worked:{title:"Signal and noise coefficients",steps:["With ᾱ=0.64, the signal coefficient is 0.8 and the noise coefficient is 0.6.","For x₀=1 and ε=−0.5, xₜ=0.8−0.3=0.5.","Knowing the exact ε lets us recover x₀=(0.5−0.6×(−0.5))/0.8=1. A learned model does not receive this oracle noise at sampling time."]},lab:"diffusion"},
 {id:"training",title:"Predict the noise using time as context",paragraphs:["Sample a training observation, a time index, and fresh Gaussian noise. Construct x_t with the forward formula. A neural network receives x_t and t and predicts the added noise. The standard simple objective is squared prediction error.","Time conditioning tells the network how corrupted its input is. Without it, the same observed value could require different denoising behavior at different noise levels. Ensure schedule coefficients broadcast across features, not accidentally across batch members."],formula:"Lsimple = Ex₀,t,ε [ ||ε − εθ(xₜ,t)||² ]",code:"t = torch.randint(0, T, (len(x0),))\na_bar = alpha_bar[t].view(-1, 1)  # [N,1] for vector data\nnoise = torch.randn_like(x0)\nxt = a_bar.sqrt()*x0 + (1-a_bar).sqrt()*noise\nloss = F.mse_loss(model(xt, t), noise)\n# For [N,C,H,W] images, coefficients need shape [N,1,1,1]."},
 {id:"reverse",title:"Denoising is a scheduled reverse transition",paragraphs:["A DDPM sampler starts near the terminal noise distribution and runs backward. At each step it uses the noise predictor to form a reverse mean, then adds the prescribed reverse variance except at the final step. Simply subtracting the full predicted noise repeatedly is not the DDPM update.","Below, equations use mathematical times 1…T and ᾱ₀=1. The downloadable script stores those transitions at array indices 0…T−1; its previous cumulative product is therefore [1, ᾱ₁,…]. State this convention to avoid off-by-one errors."],formula:"μθ(xₜ,t) = [xₜ − βₜ εθ(xₜ,t)/√(1−ᾱₜ)] / √αₜ\nβ̃ₜ = βₜ (1−ᾱₜ₋₁)/(1−ᾱₜ)\nxₜ₋₁ = μθ(xₜ,t) + √β̃ₜ z, z~N(0,I), for t>1\nx₀ = μθ(x₁,1) at the final step"},
 {id:"prediction",title:"What a predicted clean observation means",paragraphs:["A predicted noise vector can be converted into a predicted clean observation. With oracle noise from the forward construction this exactly recovers the original, up to floating-point error. With a learned estimate it is a model prediction and may be wrong.","At high noise, many original observations could explain the same noisy input. The model learns a conditional denoising relationship rather than magically identifying which historical sample produced it. Under the noise-prediction setup, the optimal noise prediction is related to the noisy-data score by a negative scale factor."],formula:"x̂₀ = [xₜ − √(1−ᾱₜ) εθ(xₜ,t)] / √ᾱₜ\nAt the optimal predictor:\n∇xₜ log pₜ(xₜ) = −E[ε | xₜ] / √(1−ᾱₜ)",takeaway:"An oracle algebra check validates the schedule and broadcasting. It does not establish sample quality."},
 {id:"architecture",title:"Condition a network at several scales",paragraphs:["For images, a U-Net combines downsampling, broad context, and upsampling, passing features between matching resolutions. Such skips are often concatenations; they are not automatically the same operation as a residual addition. Time embeddings modulate the model so it can adapt across noise levels.","The toy experiment uses a small MLP because two-dimensional points do not need an image hierarchy. This keeps the forward and reverse mechanisms inspectable without claiming to reproduce the quality or cost of an image diffusion system."],table:{headers:["Part","Purpose","Check"],rows:[["Noise schedule","Define corruption levels","ᾱT and numerical ranges"],["Time embedding","Identify the current noise level","Training and sampling use the same convention"],["Denoiser","Predict noise or an equivalent target","Target parameterization matches the sampler"],["Reverse variance","Control stochastic reverse steps","No added noise after the final transition"]]}},
 {id:"conditioning",title:"Classifier-free guidance needs conditional training",paragraphs:["A conditional model can receive a label, text representation, or other context. Classifier-free guidance combines conditional and unconditional predictions. The model must have learned an unconditional path, commonly through random condition dropping during training.","For the formula below, w=0 gives the unconditional prediction and w=1 gives the conditional one. Larger values extrapolate their difference. This can improve condition adherence while reducing diversity or introducing artifacts. The unconditional toy script does not claim to implement guided image generation."],formula:"εguided = εunconditional + w(εconditional − εunconditional)"},
 {id:"evaluation",title:"Separate regression quality from generation quality",paragraphs:["A lower training noise loss does not guarantee uniformly better samples. Difficulty varies across noise levels, and the reverse trajectory repeatedly uses model estimates. Inspect loss by time range, sample diversity, distribution coverage, and sampling cost.","A VAE can decode one latent draw in one pass; a GAN can generate in one generator pass; ordinary DDPM sampling requires many denoiser calls. Compare quality and coverage together with that cost. Faster samplers are separate methods with their own assumptions, not simply arbitrary step removal from a DDPM loop."]}
 ],pitfalls:[["Subtract predicted noise without schedule factors","Use the reverse mean and variance consistent with the training parameterization."],["A batch coefficient has the wrong shape","Reshape [N] to [N,1] or [N,1,1,1] before multiplying observations."],["The terminal distribution is still mostly signal","Check ᾱT before starting sampling from standard normal."],["Guidance without unconditional training","An unconditional branch must be learned; dropping context only at sampling can be out of distribution."]],
 practice:[["For ᾱ=0.25, x₀=2, ε=0, compute x_t.","The signal coefficient is 0.5, so x_t=1. A different noise realization would add √0.75 times ε."],["Why is the final posterior variance zero in the stated schedule?","At mathematical t=1, ᾱ₀=1, so the numerator 1−ᾱ₀ is zero. The final sample uses the reverse mean without additional noise."],["What does guidance weight w=1 mean here?","The unconditional terms cancel, leaving the conditional prediction. Some sources use a differently shifted weight convention, so inspect the formula."],["Design a generative comparison across Weeks 9–11.","Use the same toy distribution and declared budgets, compare fresh sample coverage and distance metrics, show diverse outputs, and report training and sampling costs. Do not rank the methods from a single favorable seed."]],
 review:[["Why can training jump directly to time t?","The composed Gaussian forward process has a closed-form conditional given x₀."],["What is the target in noise-prediction training?","The exact sampled Gaussian noise used to construct that training input."],["Why does the denoiser need t?","The corruption level changes the appropriate conditional prediction."],["Is an x₀ estimate an exact inversion?","Only with the corresponding oracle noise; learned estimates are uncertain and imperfect."],["Why is sampling iterative?","Each reverse transition updates the current noisy state before the next denoiser call."],["What does ᾱ measure?","The cumulative retained signal power coefficient in the forward marginal."],["What does guidance trade off?","Stronger condition adherence can come at the cost of diversity or artifacts."],["What should accompany sample images?","Coverage, diversity, compatible quantitative metrics, and training/sampling costs."]],
 experiment:{title:"Train and sample a complete toy DDPM",description:"Train a time-conditioned noise predictor on an eight-mode point distribution and sample it with the full reverse mean and posterior-variance schedule. A direct oracle inversion assertion checks the forward math. Generated points are exported to CSV for comparison with the VAE and GAN examples.",checks:["Read the reported terminal ᾱ and verify the oracle inversion error is tiny.","Compare fixed validation noise-prediction loss with training progress.","Plot generated points and inspect all modes, including missing or blurred ones.","Report the number of denoiser evaluations per sample alongside sample quality."]},bridge:"Generating plausible outputs does not tell us when a model is trustworthy. Next we measure confidence, probe explanations, and test what changes under stress.",references:[{title:"Ho et al. — Denoising Diffusion Probabilistic Models",url:"https://arxiv.org/abs/2006.11239",note:"Forward corruption, noise prediction, and DDPM sampling."},{title:"Ho & Salimans — Classifier-Free Diffusion Guidance",url:"https://arxiv.org/abs/2207.12598",note:"Combining conditional and unconditional denoising predictions."}]
};
