import type { CourseLessonData } from "./types";
export const week10: CourseLessonData = {
 week:10,title:"Generative Adversarial Networks",lead:"A generator proposes samples; a discriminator learns to distinguish them from data. Both change during training, so success depends on the interaction between two learning systems.",prerequisites:"Binary cross-entropy, logits, gradients, sampling, and generative evaluation.",outcomes:["Derive discriminator and generator objectives and their gradient differences.","Implement alternating updates with correct detach and freeze behavior.","Diagnose mode collapse without relying only on loss curves.","Evaluate sample quality and diversity under a declared protocol."],
 sections:[
 {id:"game",title:"Two models with different jobs",paragraphs:["A generator G maps random noise to a candidate observation. A discriminator D estimates whether an input came from the data or the generator under the training mixture. There is no paired target observation for a particular noise vector; the objective compares distributions through a learned critic.","Each update changes the problem faced by the other model. A discriminator loss that improves may mean it has learned a useful distinction, or that the generator has fallen behind. Neither loss is a direct measure of perceptual quality or full distribution coverage."],formula:"z ~ p(z), fake = Gθ(z)\nOriginal game: minG maxD Edata[log D(x)] + Ez[log(1−D(G(z)))]"},
 {id:"discriminator",title:"Train the discriminator on real and detached fake samples",paragraphs:["Using a discriminator that returns logits, binary cross-entropy with logits combines sigmoid and the log terms stably. Real examples receive target one and generated examples target zero. During the discriminator update, detach generated samples so the backward pass does not update the generator.","Batch balance, label conventions, preprocessing, and gradient resets must be explicit. If fake and real inputs have different scaling or trivial artifacts, D may solve an easier problem than the one you intended."],code:"opt_d.zero_grad()\nfake = generator(noise).detach()\nloss_d = F.binary_cross_entropy_with_logits(discriminator(real), torch.ones(N,1))\nloss_d += F.binary_cross_entropy_with_logits(discriminator(fake), torch.zeros(N,1))\nloss_d.backward()\nopt_d.step()"},
 {id:"generator",title:"Why the non-saturating objective helps",paragraphs:["The original minimax generator minimizes log(1−D(fake)). When the discriminator confidently rejects fake samples, the derivative with respect to its fake logit is small. The common non-saturating alternative minimizes −log D(fake), giving a larger logit-level signal in that regime.","This improves one part of the gradient path, not every part. The discriminator's input Jacobian and generator Jacobian still determine the parameter gradient. A large logit derivative cannot rescue an uninformative or poorly conditioned mapping everywhere."],formula:"Let a be the fake logit, D(fake)=sigmoid(a).\nMinimax LG = log(1−sigmoid(a)); dLG/da = −sigmoid(a)\nNon-saturating LG = −log(sigmoid(a)); dLG/da = sigmoid(a)−1",worked:{title:"A discriminator that strongly rejects the fake",steps:["At a=−5, D(fake)≈0.00669.","The minimax derivative is about −0.00669; the non-saturating derivative is about −0.99331.","Gradient descent pushes the fake logit upward in either case, but their immediate signal magnitudes differ greatly."]},lab:"gan"},
 {id:"gradient-routing",title:"Freeze weights without cutting the input gradient",paragraphs:["During a generator step, discriminator parameters can be frozen while gradients still flow through the discriminator to its input and then to G. Wrapping D in no_grad() would cut that required path. Detaching the fake during this step would also prevent G from learning.","requires_grad controls parameter gradients; train/eval controls mode-dependent behavior. BatchNorm buffers can still change while parameters are frozen. Decide whether D's running state should update during generator steps. The supplied toy MLP avoids BatchNorm so this issue is visible conceptually without hidden state changes."],code:"for p in discriminator.parameters():\n    p.requires_grad_(False)\nopt_g.zero_grad()\nfake = generator(torch.randn(N, noise_dim))\nloss_g = F.binary_cross_entropy_with_logits(discriminator(fake), torch.ones(N,1))\nloss_g.backward()  # gradient passes through D to G\nopt_g.step()\nfor p in discriminator.parameters():\n    p.requires_grad_(True)"},
 {id:"architecture",title:"Convolutional and conditional generation",paragraphs:["Image generators can progressively increase spatial resolution using upsampling plus convolution or transposed convolution. A transposed convolution is not generally the inverse of a convolution. Uneven overlap can produce checkerboard artifacts, so inspect the upsampling design and actual outputs.","A conditional GAN supplies context, such as a class label, to both G and D. The discriminator must judge whether an observation matches the condition as well as the data distribution. Conditioning does not by itself create a paired reconstruction objective for each noise draw."],table:{headers:["Component","Purpose","Check"],rows:[["Generator","Map noise and optional context to samples","Output range and upsampling artifacts"],["Discriminator","Distinguish data from generated examples","No trivial preprocessing shortcut"],["Condition embedding","Specify desired context","Provided consistently to both players"],["Fixed latent set","Compare training snapshots","Complement with fresh samples for coverage"]]}},
 {id:"collapse",title:"Good-looking samples can still miss most of the data",paragraphs:["Mode collapse means the generator produces insufficient variety, perhaps mapping many noise inputs to similar outputs. A few convincing examples can conceal missing classes or modes. Compare fixed-latent snapshots, fresh samples, coverage measures, and nearest training neighbors.","Oscillating losses are not a unique diagnosis: they may reflect ordinary game dynamics, instability, or changing discriminator skill. Check gradients and sample evolution. Adjusting learning rates, update ratios, or regularization should follow a testable hypothesis rather than treating every curve fluctuation as failure."],takeaway:"Quality and diversity are separate evaluation questions. A generator must address both."},
 {id:"evaluation",title:"Choose metrics appropriate to the domain",paragraphs:["FID compares feature-distribution summaries, so its interpretation depends on feature extractor, preprocessing, domain, and sample count. It is not a universal quality number that can be compared across incompatible pipelines. Small sample estimates can also be misleading.","For a two-dimensional toy mixture, directly measure distance to known modes and coverage of those modes. For images, combine compatible distribution metrics with diverse sample inspection and duplication checks. Keep the final evaluation protocol separate from repeated tuning on its results."]}
 ],pitfalls:[["Detach fake samples in both steps","Detach for D's update; preserve the graph through D for G's update."],["Use no_grad around D during G training","Freezing D parameters is compatible with input gradients. no_grad would remove the needed gradient path."],["A low generator loss proves quality","It only reports performance against the current discriminator objective."],["A few attractive images prove coverage","Inspect many fresh samples and measure missing modes or categories."]],
 practice:[["Calculate the two fake-logit derivatives when D(fake)=0.1.","Minimax gives −0.1; non-saturating gives −0.9. Both encourage a larger fake logit under gradient descent."],["What happens if fake.detach() is used in the generator update?","There is no differentiable path from that loss to generator parameters, so G receives no useful gradient from it."],["How can all samples be plausible yet the model be poor?","The generator may emit only one valid mode while ignoring the rest of the distribution."],["Plan a GAN diagnostic report.","Record both losses, gradient norms, fixed-latent evolution, fresh sample diversity, mode coverage or compatible domain metrics, and the training/update budget. Include unfavorable examples."]],
 review:[["Does each noise vector have a target image?","Not in an ordinary GAN; distributional feedback comes through the discriminator."],["Why use logits with BCE?","The combined loss evaluates sigmoid/log terms in a numerically stable form."],["Why detach during the D step?","To avoid propagating that discriminator-training loss into G."],["Why freeze D parameters during the G step?","To avoid accumulating their gradients while still differentiating through D's input."],["What is mode collapse?","Insufficient generated diversity or missing regions of the target distribution."],["Is transposed convolution an inverse?","Not generally; it is a related linear operation used for learned upsampling."],["What does conditional generation require?","A consistent condition supplied to the generator and discriminator, with an objective that evaluates compatibility."],["Why is FID protocol-dependent?","Features, preprocessing, domain, and sample count affect its meaning and estimate."]],
 experiment:{title:"An adversarial game on eight modes",description:"Train small MLP generator and discriminator networks on an eight-cluster ring. The script checks that generator gradients pass through a frozen discriminator, reports losses and coverage, and exports generated points to CSV. It uses a fixed training budget rather than selecting a snapshot from the final metrics.",checks:["Read the gradient-routing assertion before interpreting training behavior.","Compare the number of covered modes and distance to the nearest true mode.","Plot the sample CSV; a reasonable mean and variance can still hide collapse.","Repeat seeds and vary the D:G update budget without selecting only attractive runs."]},bridge:"Adversarial learning gets its signal from a changing discriminator. Diffusion uses known corruption noise as its training target, then generates through repeated denoising.",references:[{title:"Goodfellow et al. — Generative Adversarial Nets",url:"https://arxiv.org/abs/1406.2661",note:"The adversarial objective and non-saturating generator alternative."},{title:"Radford et al. — Unsupervised Representation Learning with DCGANs",url:"https://arxiv.org/abs/1511.06434",note:"Convolutional architecture choices for adversarial image generation."}]
};
