import type { CourseLessonData } from "./types";
export const week6: CourseLessonData = {
 week:6,title:"Recurrent Neural Networks and Memory",lead:"A sequence model must decide what to carry forward. Trace a recurrent state through time, derive its gradient problems, and see how gates create a more controllable memory path.",prerequisites:"Matrix shapes, the chain rule, gradient clipping, and residual connections.",outcomes:["Calculate a recurrent hidden-state trace.","Explain backpropagation through time and its gradient problems.","Compare RNN, LSTM, and GRU updates.","Classify variable-length sequences without treating padding as observations."],
 sections:[
 {id:"state",title:"Reuse one cell across time",paragraphs:["A recurrent neural network updates a hidden state from the current input and previous state. The same parameters are used at every step. Parameter count does not grow with sequence length, although computation and saved activations do.","The hidden state is a learned summary, not an archive of every earlier input. A many-to-one model makes one decision about a sequence; a many-to-many model makes position-wise predictions. Whether the state retains useful information must be tested."],formula:"hₜ = tanh(Wxh xₜ + Whh hₜ₋₁ + bₕ)\nlogitsₜ = Why hₜ + bᵧ",worked:{title:"Three scalar steps",steps:["Start h₀ = 0 and use hₜ = tanh(0.8xₜ + 0.5hₜ₋₁), with x = [1, 0.5, −0.5].","h₁ = tanh(0.8) ≈ 0.664; h₂ = tanh(0.4 + 0.332) ≈ 0.624.","h₃ = tanh(−0.4 + 0.312) ≈ −0.088. The final observation combines with the earlier summary."]}},
 {id:"bptt",title:"Backpropagation through the unrolled graph",paragraphs:["Backpropagation through time is ordinary reverse-mode differentiation on the unrolled recurrent computation. Because each weight is reused, its gradient accumulates contributions from every use.","Long-range sensitivity contains a product of Jacobians. Saturated tanh derivatives and small gains can shrink a signal; large gains can amplify it. Matrix directions and singular values matter, so a scalar multiplier is an illustration rather than a complete stability diagnosis."],formula:"Jₜ = ∂hₜ/∂hₜ₋₁ = diag(1 − hₜ²) Whh\n∂hT/∂hₖ = JT JT₋₁ … Jₖ₊₁",takeaway:"A long sequence creates a deep computation graph even when the cell is small."},
 {id:"clipping",title:"Control explosions and bound the graph",paragraphs:["Global norm clipping scales down a gradient when its norm exceeds a threshold. It preserves direction for that update but cannot recover a gradient that has already vanished. Clip after backward and before the optimizer step.","Truncated BPTT detaches a carried state at selected boundaries. Detaching preserves the numerical state while cutting its gradient history, reducing memory and limiting credit assignment. Resetting changes the value too; it is a different operation."],formula:"gclipped = g · min(1, c / ||g||₂)",worked:{title:"Clip [6,8] at threshold 5",steps:["The norm is √(36+64) = 10.","Multiply by 5/10 to obtain [3,4].","A threshold of 12 would leave this gradient unchanged."]},code:"loss.backward()\nnn.utils.clip_grad_norm_(model.parameters(), 5.0)\noptimizer.step()\n# LSTM state at a truncation boundary:\nh, c = h.detach(), c.detach()"},
 {id:"lstm",title:"An additive memory with gates",paragraphs:["An LSTM separates cell state c from exposed hidden state h. The forget gate controls retention, the input gate controls writing a candidate, and the output gate controls exposure. Sigmoid gates are learned functions of the current input and previous hidden state.","Holding the gate values fixed, the direct cell-state derivative is the forget gate. The total derivative also includes dependencies through gates and hidden states. Long retention is possible, not guaranteed: multiplying forget gates below one still attenuates the direct path."],formula:"fₜ, iₜ, oₜ = sigmoid(affine functions of [xₜ,hₜ₋₁])\ngₜ = tanh(Wg[xₜ,hₜ₋₁] + bg)\ncₜ = fₜ ⊙ cₜ₋₁ + iₜ ⊙ gₜ\nhₜ = oₜ ⊙ tanh(cₜ)",worked:{title:"Read, write, expose",steps:["Let cprevious = 0.8, f = 0.9, i = 0.25, g = 0.4, and o = 0.7.","Retained memory is 0.72; new writing is 0.10. The updated cell is 0.82.","The hidden state is 0.7 × tanh(0.82) ≈ 0.473. Cell and hidden state are different quantities."]},lab:"memory"},
 {id:"gru",title:"GRU: a compact gated alternative",paragraphs:["A GRU has one state with reset and update gates. The reset gate changes how the past enters the candidate, while the update gate blends old state and candidate. It has no separate LSTM-style cell state.","Here z weights the new candidate, matching the lecture convention. PyTorch calls the old-state retention weight z and uses a particular reset placement. Translate conventions before comparing a hand calculation to library output."],formula:"Lecture convention:\nh̃ₜ = tanh(Wx xₜ + Wh(rₜ ⊙ hₜ₋₁) + b)\nhₜ = (1−zₜ) ⊙ hₜ₋₁ + zₜ ⊙ h̃ₜ\nPyTorch update: hₜ = zₜ ⊙ hₜ₋₁ + (1−zₜ) ⊙ nₜ",table:{headers:["Cell","State","Tradeoff"],rows:[["RNN","h","Simple; long credit paths can be difficult."],["LSTM","h and c","Separate memory path; more gates."],["GRU","h","Compact gating; task-dependent performance."]]}},
 {id:"padding",title:"Do not invent observations through padding",paragraphs:["With batch_first=True, inputs have shape [batch,time,features]. An ordinary RNN keeps updating through padded zeros because recurrent weights and biases still act. Both output[:,−1] and h_n from an unpacked padded input can therefore include nonexistent time steps.","Pack inputs with their true lengths or implement masked updates. With a packed, single-direction LSTM, the final layer's h_n corresponds to the last real step. In a bidirectional model, concatenate the final layer's forward and backward final states. h_n[-1] alone gives only one direction. Bidirectionality is appropriate only when future context is available at inference."],code:"packed = nn.utils.rnn.pack_padded_sequence(\n    x, lengths.cpu(), batch_first=True, enforce_sorted=False\n)\n_, (h_n, c_n) = lstm(packed)\nlogits = head(h_n[-1])  # final layer, one direction"},
 {id:"generation",title:"Teacher forcing and the context bottleneck",paragraphs:["Teacher forcing feeds the correct previous output token during decoder training. At inference, a generator consumes its own earlier predictions. Errors then change later inputs, creating a mismatch between training and generation contexts.","An encoder–decoder can compress a source sequence into a final context state. Long inputs stress this fixed-size channel. Attention will allow a decoder to retrieve different information from multiple source positions.","Compare recurrent cells under fixed data splits and tuning budgets. Report results by sequence length, clipping frequency, and padding policy. Split related sequences by source or time when random rows would leak correlated information."]}
 ],
 pitfalls:[["Zero padding is harmless","Biases and recurrence still change state. Pack or mask using true lengths."],["Clipping fixes vanishing gradients","Clipping limits large norms; it cannot restore a tiny long-range signal."],["Detach means reset","Detach keeps values and removes gradient history; reset changes values."],["Bidirectional means online-ready","The backward direction uses future observations, which may be unavailable in deployment."]],
 practice:[["Calculate direct retention over 20 steps at f=0.9 and f=0.99.","The multipliers are approximately 0.1216 and 0.8179. This holds gates fixed; it is not the full recurrent Jacobian."],["What does a bidirectional LSTM with hidden size 32 output?","There are 64 features per time step. For L layers, h_n has shape [2L,batch,32]. Concatenate the final layer's two directions for a complete final representation."],["Why might two sequences with the same real values produce different final states?","If they have different padding lengths and run unpacked, the model executes different numbers of updates. Dropout or an incorrectly reused initial state can also change outputs."],["Design a memory stress test.","Make a label depend on an early cue followed by distractors. Evaluate held-out length buckets, including longer sequences, and compare cells with the same split and tuning budget."]],
 review:[["Why share weights over time?","To learn a reusable transition rule and process different sequence lengths with one parameter set."],["Is BPTT a different gradient algorithm?","It is backpropagation on an unrolled recurrent graph."],["What does the LSTM output gate control?","Exposure of tanh(cell state) as the hidden state."],["Can the cell state exceed one?","Yes; the additive cell state is not bounded by tanh."],["Why do GRU equations differ?","Update-gate meaning and reset placement vary between conventions."],["Does h_n always ignore padding?","No. Appropriately packed input avoids the padded updates; ordinary padded input does not."],["What is teacher forcing?","Using reference previous outputs as decoder inputs during training."],["Why introduce attention?","To retrieve from multiple source positions instead of relying solely on one compressed final context."]],
 experiment:{title:"Remember the first observation",description:"Compare RNN, GRU, and LSTM on variable-length sequences whose first observation determines the label. Later observations are distractors. Each model uses packed inputs, validation checkpoint selection, and a reserved test set.",checks:["Verify true lengths exclude the padding from recurrent computation.","Compare validation losses and test accuracy without assuming a universal winning cell.","Increase sequence length in a separate stress study and inspect performance by length.","Observe pre-clipping gradient norms to distinguish instability from weak long-range learning."]},bridge:"A recurrent state compresses earlier observations. Next, attention lets a model retrieve the particular positions it needs.",references:[{title:"PyTorch — LSTM",url:"https://docs.pytorch.org/docs/stable/generated/torch.nn.LSTM.html",note:"Gates, packed inputs, tensor shapes, and returned states."},{title:"PyTorch — GRU",url:"https://docs.pytorch.org/docs/stable/generated/torch.nn.GRU.html",note:"The library's update convention and reset placement."},{title:"PyTorch — pack_padded_sequence",url:"https://docs.pytorch.org/docs/stable/generated/torch.nn.utils.rnn.pack_padded_sequence.html",note:"Passing true sequence lengths through a padded batch."}]
};
