import type { CourseLessonData } from "./types";
export const week4: CourseLessonData = {
  week: 4, title: "Convolutional Neural Networks",
  lead: "A useful visual pattern should not need to be learned again at every pixel. Build a network that understands neighbourhoods, reuses detectors, and gradually combines local evidence.",
  prerequisites: "Matrix multiplication, gradients, ReLU, and the training/validation discipline from Week 3.",
  outcomes: ["Calculate a convolution by hand and track every tensor dimension.", "Explain locality, weight sharing, equivariance, and their limits.", "Compute parameters, receptive fields, and approximate convolution cost.", "Train a small CNN and defend its architecture against a dense baseline."],
  sections: [
    { id:"spatial-structure", title:"Why images need a structural assumption", paragraphs:["An image is an array with a neighbourhood structure: nearby pixels often belong to the same edge, texture, or object part. Flattening an image retains its values, but a fully connected layer does not explicitly encode which values were neighbours. It assigns a separate coefficient to every input–output connection.","A convolutional layer makes two assumptions. A local patch can contain useful evidence, and the same pattern may matter at multiple locations. Local connectivity restricts where a detector looks; weight sharing reuses that detector across the image. These are inductive biases, not universal truths. Absolute position or long-range relations may require other mechanisms."], takeaway:"Convolution changes the model's assumptions and parameterization; it does not magically add information to the image." },
    { id:"kernel", title:"From one patch to a feature map", paragraphs:["Slide a small kernel over the input. At each location, multiply corresponding entries, sum them, and add a bias. Applying the same kernel repeatedly produces one feature map. A kernel is a learned set of coefficients; its feature map depends on the current input.","Most deep learning libraries compute cross-correlation: the kernel is not flipped before the dot product. Calling it convolution is the standard convention in this setting. With several input channels, one output detector has a separate spatial kernel for each input channel and sums their contributions."], formula:"Y[o, i, j] = b[o] + Σc Σu Σv W[o,c,u,v] X[c, i·s+u−p, j·s+v−p]", worked:{title:"A vertical contrast detector",steps:["Patch: [[1, 2, 0], [0, 1, 3], [2, 1, 1]]. Kernel: [[1, 0, −1], [1, 0, −1], [1, 0, −1]]. Bias: 0.","Row contributions are 1 − 0 = 1, 0 − 3 = −3, and 2 − 1 = 1.","The output is −1. ReLU would turn this response into 0; an oppositely oriented kernel could respond positively."]}, lab:"convolution" },
    { id:"shapes", title:"Stride, padding, and output dimensions", paragraphs:["Stride determines the distance between successive window origins. Padding extends the boundary so edge locations can participate in more windows. Zero, reflection, and replication padding impose different assumptions about what lies beyond the observed image.","Dilation spaces out kernel entries. Its effective width is d(k−1)+1. Apply the output-size formula independently to height and width; floor division matters when a final partial window does not fit. A shape calculation should precede a training run."], formula:"Hout = floor((Hin + 2p − d(k−1) − 1) / s) + 1\nWout uses the same formula with Win.\nWeight shape: (Cout, Cin / groups, kh, kw)", table:{headers:["Input / kernel / padding / stride", "Output size", "Reason"],rows:[["32 / 3 / 1 / 1","32","One pixel of padding preserves size."],["32 / 5 / 0 / 1","28","No windows extend past the boundary."],["28 / 3 / 1 / 2","14","Stride reduces sampling density."],["64 / 7 / 3 / 2","32","A larger window can still halve resolution."]]} },
    { id:"channels", title:"Count parameters, then count computation", paragraphs:["For an ordinary convolution with groups = 1, each output channel sees all input channels. The trainable weights number kh × kw × Cin × Cout, plus Cout biases when enabled. The number of spatial positions does not change this parameter count.","It does change compute and activation memory. Reusing each filter at Hout × Wout locations creates many multiply–accumulate operations. A layer with few parameters can be expensive on a large image. A dense layer and a convolutional layer also produce different representations, so raw parameter counts alone are not a fair accuracy comparison."], formula:"Parameters = kh·kw·Cin·Cout + Cout\nApproximate MACs = Hout·Wout·kh·kw·Cin·Cout", worked:{title:"16 input channels → 32 output channels",steps:["A 3 × 3 convolution has 3 × 3 × 16 × 32 = 4,608 weights and 32 biases: 4,640 parameters.","At 32 × 32 output resolution it uses about 4.72 million MACs per image, ignoring bias and activation cost.","The output contains 32 × 32 × 32 = 32,768 activations per image. Training must also retain information for backpropagation."]} },
    { id:"receptive-field", title:"How small windows see larger structures", paragraphs:["A later unit combines earlier units, each of which already depends on an input neighbourhood. This composition increases the theoretical receptive field. Keep two quantities: r, the receptive-field size, and j, the distance in input pixels between adjacent feature positions.","The theoretical receptive field describes possible dependence. The effective influence of different input pixels depends on learned weights, activations, and data. A large theoretical field does not prove a model uses distant context."], formula:"Start: r₀ = 1, j₀ = 1\nrℓ = rℓ₋₁ + (kℓ − 1)·dℓ·jℓ₋₁\njℓ = jℓ₋₁·sℓ", table:{headers:["Layer", "Receptive field r", "Jump j"],rows:[["3×3 convolution, stride 1","3","1"],["3×3 convolution, stride 1","5","1"],["2×2 pool, stride 2","6","2"],["3×3 convolution, stride 1","10","2"]]} },
    { id:"pooling", title:"Downsampling, equivariance, and invariance", paragraphs:["Max pooling selects the largest local response; average pooling summarizes local response strength. A strided convolution instead learns its reduction. All three reduce spatial resolution, which saves later computation but can remove fine detail or alias high-frequency patterns.","Away from boundaries, stride-one convolution is translation equivariant: translating the input translates the feature map. Invariance means the output does not change. Pooling and global aggregation can encourage some tolerance to position, but arbitrary translations, padding, and stride prevent a blanket guarantee of invariance."], table:{headers:["Operation", "What it preserves or learns", "What to watch"],rows:[["Max pooling","Presence of a strong local response","Weak but useful details can disappear."],["Average pooling","Average local evidence","Small sharp features may be diluted."],["Strided convolution","A learned spatial reduction","More parameters; aliasing remains possible."],["Global average pooling","One mean per channel","Discards precise location information."]]} },
    { id:"cnn-design", title:"Build a complete classifier", paragraphs:["Separate a feature extractor from a prediction head. For a 28 × 28 grayscale image, two padded 3 × 3 convolutions with 2 × 2 pooling leave a 32 × 7 × 7 tensor. Flatten only after the convolutional stages, then produce one logit per class.","Use cross-entropy on raw logits with integer class labels. During training, inspect shapes, loss, and validation errors. Feature maps can reveal dead filters or unexpected responses, but a bright activation is not proof that a filter represents a named human concept."],code:"model = nn.Sequential(\n    nn.Conv2d(1, 16, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),\n    nn.Conv2d(16, 32, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),\n    nn.Flatten(), nn.Linear(32 * 7 * 7, 10),\n)\n# images: [batch, 1, 28, 28]; labels: [batch], dtype long\nloss = F.cross_entropy(model(images), labels)",takeaway:"Choose the smallest architecture that tests your hypothesis, then use validation evidence to justify complexity." },
  ],
  pitfalls:[["A flattening mismatch","Write down channels × height × width before Linear. Changing a stride changes the head's input size."],["An augmentation changes the label","A vertical/horizontal stripe task cannot use a 90° rotation while keeping its label. Check the task semantics first."],["The parameter count tells the whole story","Report resolution, MACs or measured runtime, and activation memory alongside parameter count."],["A beautiful feature map proves understanding","Inspect held-out mistakes and controlled perturbations; visualization alone does not establish causal reliance."]],
  practice:[["Design a 64 × 64 RGB classifier with fewer than 250,000 parameters.","One option is padded 3×3 convolutions 3→16→32→64, each followed by ReLU and 2×2 pooling; global average pooling; and Linear(64,10). With biases: 448 + 4,640 + 18,496 + 650 = 24,234 parameters. Explain whether losing location information is appropriate for your task."],["What does dilation 2 do to a 3 × 3 kernel on a 32 × 32 input?","Its effective width is 5. With stride 1 and padding 2, output size remains 32. It still has nine spatial weights per input/output channel pair, not 25."],["Why can a CNN still fail after an image shifts by one pixel?","Stride and pooling change which samples are grouped. Padding and cropping alter boundary evidence. Equivariance for an ideal stride-one operator does not imply invariance of the whole classifier."],["Plan a fair dense-versus-CNN experiment.","Fix data splits, preprocessing, optimizer search budget, and stopping criteria. Compare validation-selected models, parameters, runtime, and error types; report the reserved test result once. Repeat with multiple seeds before claiming a reliable advantage."]],
  review:[["What is shared in weight sharing?","The same kernel coefficients and output-channel bias are used at each spatial location."],["Does one filter look at only one input channel?","For ordinary convolution it spans every input channel; depthwise and grouped convolutions alter that connectivity."],["What is the difference between a kernel and a feature map?","The kernel is a parameter tensor. The feature map is an input-dependent activation tensor produced by applying it."],["Does padding add measured information?","No. It supplies an assumed boundary extension, such as zeros or reflected values."],["Why stack small kernels?","Composition increases receptive field and inserts nonlinearities, while often using fewer weights than a single wide kernel at comparable channel widths."],["Why can fewer parameters still mean high computation?","Each shared parameter is reused across many output positions, and those operations must still be executed."],["When might a CNN be the wrong bias?","When local translation patterns are unhelpful or absolute positions and distant interactions dominate, other architectures or added positional mechanisms may fit better."],["What should you inspect besides accuracy?","Loss curves, per-class mistakes, sensitivity to transformations, parameter count, runtime, and memory."]],
  experiment:{title:"A CNN learns orientation",description:"Train a two-stage CNN on noisy 16 × 16 horizontal and vertical stripes. The script creates fixed train, validation, and test sets, selects a checkpoint using validation loss, and reports the final test accuracy plus the feature shapes and parameter count.",checks:["The output must have shape [batch, 2], with finite cross-entropy and gradients.","Compare validation loss before and after training; a smoke run only checks execution.","Shift the stripe or increase noise in a separate stress set. State which change should preserve its label.","Replace the feature extractor with a dense baseline and keep the split and tuning budget fixed."]},
  bridge:"We can now build spatial hierarchies. Next, we ask why making them deeper can make training worse—and how residual connections and pretrained features help.",
  references:[{title:"PyTorch: Conv2d",url:"https://docs.pytorch.org/docs/stable/generated/torch.nn.Conv2d.html",note:"Exact channel, kernel, stride, padding, dilation, and output-shape conventions."},{title:"LeCun et al. — Gradient-Based Learning Applied to Document Recognition",url:"https://doi.org/10.1109/5.726791",note:"A foundational example of trainable convolutional networks for document recognition."}],
};
