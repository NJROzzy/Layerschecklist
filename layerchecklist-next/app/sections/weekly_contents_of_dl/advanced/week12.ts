import type { CourseLessonData } from "./types";
export const week12: CourseLessonData = {
 week:12,title:"Interpreting and Stress-Testing Black Boxes",lead:"Accuracy answers only one question. A useful model also needs confidence estimates we can assess, explanations we can challenge, and evidence about where its behavior breaks.",prerequisites:"Classification probabilities, held-out evaluation, gradients, and distribution shift.",outcomes:["Distinguish accuracy, calibration, and uncertainty.","Fit temperature scaling on a separate calibration split.","Interpret attribution methods without treating them as causal proof.","Build a reproducible stress report and a model card with clear limits."],
 sections:[
 {id:"confidence",title:"A confident prediction can still be wrong",paragraphs:["Accuracy measures the fraction of correct decisions. Calibration asks whether stated probabilities agree with observed frequencies. Among predictions assigned confidence near 0.8, roughly 80% should be correct for that confidence interpretation to hold in the evaluated population.","Softmax normalizes logits; it does not certify that an input is familiar or that the model understands its own uncertainty. A model can be highly confident on out-of-distribution inputs. Entropy summarizes one predictive distribution, but low entropy does not prove reliability."],formula:"Accuracy = mean[ predicted label = true label ]\nEntropy(p) = −Σc pc log pc\nNLL = −mean[log p(true class)]"},
 {id:"calibration",title:"Measure confidence against outcomes",paragraphs:["A reliability diagram bins predictions by confidence and compares each bin's mean confidence with its empirical accuracy. Expected calibration error weights each bin's absolute gap by its fraction of examples. Empty bins contribute zero.","ECE depends on binning and sample count and can conceal subgroup failures. Report bin counts and supplement it with a proper scoring rule such as negative log-likelihood or Brier score. Small differences on a tiny test set should not be interpreted as precise reliability improvements."],formula:"ECE = Σb (nb/N) · | accuracy(b) − confidence(b) |",worked:{title:"Two equally sized bins",steps:["Suppose 50 predictions average confidence 0.6 with accuracy 0.5, and 50 average 0.9 with accuracy 0.7.","ECE = 0.5×|0.5−0.6| + 0.5×|0.7−0.9| = 0.15.","This is a confidence calibration summary for these bins, not a probability that any particular prediction is wrong."]},lab:"calibration"},
 {id:"temperature",title:"Adjust confidence without changing the winning class",paragraphs:["Temperature scaling divides logits by a positive scalar T. Fit T by minimizing calibration-set NLL after freezing the trained model. T>1 softens probabilities; 0<T<1 sharpens them. A positive scalar preserves logit ordering and therefore argmax, apart from numerical or tie-handling effects.","Use separate data for fitting model parameters, selecting model settings, fitting temperature, and final testing when feasible. Fitting T on the final test set would contaminate the reported result. Calibration learned in one distribution may fail after a shift."],formula:"pT(y=c|x) = softmax(logits(x)/T)c, T>0\nFit log(T) on calibration data; keep the classifier fixed.",code:"# logits_cal are detached outputs from the frozen model.\nlog_temperature = nn.Parameter(torch.zeros(()))\nT = log_temperature.exp()\ncalibration_loss = F.cross_entropy(logits_cal / T, labels_cal)"},
 {id:"uncertainty",title:"Different sources of uncertainty",paragraphs:["Aleatoric uncertainty describes variability inherent in observations or labels under a model of the task. Epistemic uncertainty describes lack of knowledge about the model or its parameters. These distinctions are useful, but exact decompositions depend on assumptions and cannot be read directly from one softmax score.","Ensembles can reveal disagreement by averaging predicted probabilities from independently trained models. Monte Carlo dropout uses repeated stochastic predictions, but activating the whole model with train() also changes BatchNorm behavior. Enable the intended dropout layers while keeping BatchNorm in evaluation mode."],takeaway:"Uncertainty estimates require their own validation under the conditions in which decisions will be made."},
 {id:"attribution",title:"Ask what an explanation actually measures",paragraphs:["Input gradients measure local sensitivity of a chosen output to small input changes. Integrated gradients aggregate gradients along a path from a chosen baseline. Occlusion removes or replaces a region and measures the prediction change. Each answers a different intervention question.","Baselines, correlated features, saturation, and unrealistic perturbations can change conclusions. An occluded image may lie outside the training distribution. A saliency map is not proof of a causal mechanism in the world. Check whether explanations change appropriately when labels or model weights are randomized, and compare more than one view of the evidence."],formula:"Integrated gradient for coordinate i:\nIGᵢ(x) = (xᵢ−x′ᵢ) ∫₀¹ ∂F(x′+α(x−x′))/∂xᵢ dα\nx′ is the chosen baseline; F is a specified model output."},
 {id:"stress",title:"Make failure conditions explicit",paragraphs:["Test corruptions, acquisition changes, subgroup slices, sequence lengths, and other shifts relevant to the intended use. Record exactly which transformation was applied, its strength, and whether the label should remain valid. Report group counts and variation rather than averaging away the worst cases.","A bounded adversarial perturbation can test local sensitivity of your own model. FGSM takes one step in the sign of the input loss gradient, with a declared norm budget and valid input bounds. The budget must be expressed in the correct input units, especially when inputs are normalized. One attack is a local test, not a general robustness certificate."],formula:"xadv = clip_to_valid_domain(x + ε · sign(∇x L(model(x),y)))\nReport ε, input scale, clipping, and the evaluated model.",table:{headers:["Test","Question","Report"],rows:[["Noise or blur","Can predictions tolerate plausible corruption?","Strength curve and label validity"],["Subgroup slice","Does aggregate accuracy hide uneven errors?","Sample count and per-group metrics"],["Acquisition/domain shift","Does the learned relationship transfer?","Source/target definitions and confidence gaps"],["Local adversarial test","Can small input changes alter decisions?","Budget, attack settings, and clean baseline"]]}},
 {id:"model-card",title:"Turn observations into an honest model card",paragraphs:["A model card records intended use, unsuitable uses, data provenance, preprocessing, training setup, evaluation splits, overall and sliced metrics, calibration, stress behavior, cost, and known limitations. It should make the evidence reviewable rather than merely describe the architecture.","A useful conclusion connects a claim to a result and its scope. For example: temperature scaling reduced NLL on this held-out distribution while preserving accuracy; under the declared corruption, both accuracy and calibration worsened. Such a statement is more informative than calling the model trustworthy without qualification."]}
 ],pitfalls:[["Calibrating on the final test set","Fit temperature on separate calibration data and report final metrics on untouched data."],["ECE is a complete reliability score","Binning, sample size, and aggregation can hide important failures."],["Turn on all training layers for MC dropout","Keep BatchNorm evaluation behavior while selectively enabling dropout."],["A heat map proves causality","Attribution depends on output choice, baseline, local behavior, and assumptions about interventions."]],
 practice:[["Does T=2 change the predicted class for logits [3,1,−2]?","No. Dividing by positive 2 preserves the ordering; it makes the distribution less sharp."],["Compute ECE for 20 predictions at confidence 0.7 with 12 correct.","For that one bin, accuracy is 12/20=0.6 and ECE=|0.6−0.7|=0.1. The small sample limits the precision of the estimate."],["Why might an occlusion result be misleading?","The replacement may create an unrealistic input, correlated features may compensate, and the result depends on which region and baseline are chosen."],["Write a minimum stress report.","Declare intended use, clean baseline, transformations and strengths, label-validity assumptions, group counts, accuracy/NLL/calibration, uncertainty across runs or samples, and failure examples."]],
 review:[["Can accuracy stay fixed while NLL changes?","Yes. Probabilities may change without changing the winning class."],["Does a high softmax probability detect familiarity?","No. Out-of-distribution inputs can still receive confident predictions."],["What does temperature scaling learn?","One positive scalar that adjusts logit sharpness on calibration data."],["Why report bin counts?","A large calibration gap supported by very few observations is uncertain."],["How do ensembles combine classifications?","A common predictive mixture averages probabilities, not raw logits."],["What does an input gradient measure?","Local sensitivity of a specified output to input changes at the current point."],["Does passing FGSM prove robustness?","No. It establishes behavior under one specified test, not all perturbations or shifts."],["What makes a model card useful?","Concrete evidence, clear data and evaluation protocols, intended scope, and explicit observed limitations."]],
 experiment:{title:"Calibrate a classifier, then stress it",description:"Train a small classifier on synthetic two-dimensional data using distinct training, validation, calibration, and test splits. Fit temperature only on calibration logits, compare clean test metrics before and after scaling, and evaluate controlled feature noise plus a bounded FGSM perturbation of this local model.",checks:["Confirm positive temperature leaves test argmax predictions unchanged.","Read accuracy, NLL, ECE, and bin counts together.","Compare clean and stressed results; calibration is not guaranteed to transfer.","Use the printed metrics to write a model card with the exact split and perturbation settings."]},bridge:"We have trained models from labeled targets and generative objectives, then examined their reliability. The final week changes the source of feedback: an agent acts, receives rewards, and changes what it observes next.",references:[{title:"Guo et al. — On Calibration of Modern Neural Networks",url:"https://arxiv.org/abs/1706.04599",note:"Calibration measures and temperature scaling."},{title:"Sundararajan et al. — Axiomatic Attribution for Deep Networks",url:"https://arxiv.org/abs/1703.01365",note:"Integrated gradients and the role of a baseline."},{title:"Mitchell et al. — Model Cards for Model Reporting",url:"https://arxiv.org/abs/1810.03993",note:"Structured reporting of performance, intended use, and limitations."}]
};
