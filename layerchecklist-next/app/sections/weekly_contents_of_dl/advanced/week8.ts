import type { CourseLessonData } from "./types";
export const week8: CourseLessonData = {
 week:8,title:"Representation Learning, Self-Supervision, and Pretraining",lead:"Before learning a decision boundary, a network can learn a useful way to represent its inputs. The training signal can come from missing content, another view, or the next element of a sequence.",prerequisites:"Embeddings, cross-entropy, CNNs, transformers, and transfer learning.",outcomes:["Distinguish representation learning, self-supervision, and downstream learning.","Construct a contrastive objective with correct positives and masking.","Explain how augmentations define desired invariances.","Evaluate frozen features and fine-tuning without leaking held-out information."],
 sections:[
 {id:"representations",title:"Learn coordinates that make a task easier",paragraphs:["A representation h=fθ(x) maps a complex input into features useful for later decisions. A good embedding can make related inputs nearby, expose predictive factors, or support a simple downstream classifier. Individual coordinates need not correspond to named concepts; useful structure can be distributed across many dimensions.","Training loss alone does not tell us whether a representation transfers. A model might solve its pretraining task using a shortcut. Evaluate it on declared downstream tasks, with held-out data and baselines that reveal what pretraining contributed."],formula:"Input x → encoder fθ → representation h → task head gψ → prediction"},
 {id:"targets",title:"Self-supervision creates targets from observations",paragraphs:["Self-supervised learning uses supervision constructed from the data rather than requiring a person to label every example. Masked prediction hides content and asks the model to recover it. Autoregressive prediction uses earlier elements to predict a later one. Denoising predicts clean content from a corrupted observation.","Contrastive learning instead compares related and unrelated views. These objectives ask different questions and encourage different information to survive. Self-supervised does not mean target-free, bias-free, or automatically useful for every task."],table:{headers:["Objective","Constructed target","Possible shortcut or limitation"],rows:[["Masked prediction","Hidden token or patch","Recovering local texture without global meaning"],["Autoregression","Next element","Learning frequent patterns without robust reasoning"],["Denoising","Clean observation","Fitting a corruption unrelated to deployment"],["Contrastive learning","Identity of a related view","Using accidental view-specific clues"]]}},
 {id:"views",title:"Augmentations define what may be ignored",paragraphs:["Two views of the same example are treated as a positive pair. The model is encouraged to keep information shared by those views and to tolerate their differences. This means augmentation is part of the learning objective, not merely cosmetic preprocessing.","If a crop removes the labeled object, two views may no longer have the same semantic meaning. If color is the target signal, aggressive color jitter can erase it. Two different examples can also represent the same semantic class: treating them as negatives introduces false negatives. Inspect views, duplicates, and data provenance before tuning the loss."],takeaway:"Every invariance is a decision about which information the model is allowed to discard."},
 {id:"contrastive",title:"Pull a positive closer, compete with alternatives",paragraphs:["A SimCLR-style pipeline maps inputs to h with an encoder and to z with a projection head. Normalize z before cosine similarity. For each anchor, the other view of the same example is positive; all other views except the anchor itself participate in the denominator.","Temperature controls score sharpness. A low value emphasizes difficult competitors and can create large, concentrated gradients. Both directions of every positive pair should contribute. The projection head serves the pretraining objective; downstream evaluation commonly uses the encoder representation h."],formula:"sᵢⱼ = cosine(zᵢ,zⱼ) / τ\nℓᵢ = −log[ exp(sᵢ,positive) / Σk≠i exp(sᵢk) ]\nL = mean over all 2N anchors",worked:{title:"What collapse looks like to this loss",steps:["A batch of N examples creates 2N views. Each anchor has 2N−1 candidate keys after excluding itself.","If every normalized representation is identical, every candidate score is equal.","The positive probability is 1/(2N−1), so the loss is log(2N−1). For N=4 it is log(7) ≈ 1.946.","A lower loss indicates better discrimination within this batch, but does not by itself prove useful semantic transfer."]},lab:"contrastive"},
 {id:"implementation",title:"Get the pair indices and masks right",paragraphs:["Concatenate first views followed by second views. Then anchor i's positive is (i+N) modulo 2N. Mask only the diagonal self-comparison, not the positive. Cross-entropy can treat the other-view index as the correct class.","A shape-correct implementation can still be semantically wrong if the two view batches are shuffled independently. Keep their correspondence explicit. Monitor representation variability and retrieval examples as well as the scalar loss."],code:"h1, h2 = encoder(view1), encoder(view2)\nz = F.normalize(projector(torch.cat([h1,h2])), dim=1)\nN = len(view1)\nlogits = z @ z.T / temperature\nlogits.fill_diagonal_(float('-inf'))\npositive = (torch.arange(2*N, device=z.device) + N) % (2*N)\nloss = F.cross_entropy(logits, positive)"},
 {id:"evaluation",title:"Separate pretraining from downstream evidence",paragraphs:["A linear probe freezes the encoder and fits a linear classifier using labeled training data. Fine-tuning updates some or all encoder weights too. These answer different questions: how accessible are the features already, and how well can they adapt?","Use the same downstream split, label budget, head training procedure, and validation budget when comparing pretrained and random encoders. Put a frozen encoder in evaluation mode when BatchNorm or dropout would otherwise change its behavior. Test labels must not select the pretraining checkpoint or hyperparameters.","Using unlabeled test inputs during pretraining is still access to the test distribution. It may be a valid transductive design if explicitly declared, but it differs from evaluating generalization to unseen examples. The supplied experiment excludes those inputs."],table:{headers:["Evaluation","Updated parameters","Question"],rows:[["Linear probe","Linear head only","Are useful factors linearly accessible?"],["Feature extraction","Chosen task head","Can fixed features support this task?"],["Fine-tuning","Head and selected encoder layers","Can the representation adapt?"],["Random-encoder baseline","Head only","How much did pretraining add?"]]}},
 {id:"pretraining",title:"From one task to broad reuse",paragraphs:["Pretraining transfers parameters, not a ready-made answer to every downstream problem. Broader data, capacity, and compute can support broader reuse, but data quality, objective choice, and evaluation coverage remain decisive. A pretrained model is not automatically a foundation model simply because its weights were saved.","A two-dimensional embedding plot is a visualization of a projection, not proof of class separation in the original space. Report quantitative probe or retrieval results, label-efficiency curves, domain-shift tests, and collapse diagnostics such as per-dimension variation. Avoid interpreting a pleasing cluster plot as the entire evaluation."]}
 ],pitfalls:[["The positive was masked out","Exclude the anchor itself from the denominator, while keeping its paired view."],["Independent shuffles broke the pairs","Augment corresponding examples without losing their shared indices."],["Good contrastive loss guarantees good labels","The objective may preserve instance details unrelated to the downstream task."],["Unlabeled test inputs cannot leak information","They reveal test-distribution information. Declare transductive access or keep them out."]],
 practice:[["For N=16 examples, what is the collapsed uniform loss?","There are 32 views and 31 candidates per anchor. The loss is log(31) ≈ 3.434."],["Why can rotation help one task and hurt another?","Orientation may be a nuisance for some object identities but the label itself for an orientation task. The augmentation must match the intended invariance."],["Design a fair linear-probe comparison.","Freeze pretrained and random encoders, use the same labeled split and head optimization budget, choose checkpoints on validation, and report held-out accuracy. Repeat seeds and label budgets."],["How would you detect collapse beyond loss?","Inspect feature standard deviations, covariance spectrum or effective rank, pairwise similarities, and probe performance. Constant embeddings have little variability and cannot distinguish examples."]],
 review:[["Is self-supervised learning unsupervised in the sense of no targets?","No. It constructs targets from observations or relationships between views."],["What is a representation?","A learned feature encoding of an input, often reused by a downstream head."],["Why use a projection head?","It provides a space specialized for the pretraining loss while leaving the encoder features available for transfer."],["What is a false negative?","A candidate treated as unrelated even though it shares relevant semantics with the anchor."],["What does temperature change?","The sharpness of the similarity distribution and which competitors dominate the loss."],["What does a linear probe isolate?","The usefulness of a frozen representation to a linear decision rule."],["Can a visualization prove transfer?","No. Projection artifacts and selected examples can mislead; use held-out tasks and metrics."],["Why record the pretraining dataset?","It determines data exposure, potential overlap, learned biases, and the fairness of comparisons."]],
 experiment:{title:"Pretrain features, then freeze and probe",description:"Contrastively pretrain an encoder on noisy views of unlabeled synthetic clusters. Then train linear probes on a small labeled training set for both the pretrained encoder and a random encoder. Validation selects each probe; a separate test set supplies the final comparison.",checks:["Check positive indices and diagonal masking on a small batch before trusting loss.","Compare feature standard deviations against a collapsed constant representation.","Report both probes under the same label and optimization budget.","Increase augmentation noise until it crosses cluster boundaries and explain the effect on semantics."]},bridge:"Self-supervision learns from structure already present in data. Next we reconstruct observations and introduce a probabilistic latent space that can also generate new ones.",references:[{title:"Chen et al. — A Simple Framework for Contrastive Learning of Visual Representations",url:"https://arxiv.org/abs/2002.05709",note:"SimCLR, contrastive pairs, augmentation, and projection heads."},{title:"Devlin et al. — BERT",url:"https://arxiv.org/abs/1810.04805",note:"Masked language modeling as a pretraining objective for bidirectional representations."}]
};
